name: pages
on:
  push:
    branches: [main]
  workflow_dispatch:

# Least privilege for the build; the Pages deployment needs to write the
# deployment and mint an OIDC token for actions/deploy-pages.
permissions:
  contents: read
  pages: write
  id-token: write

# Only one Pages deployment at a time. In-progress production deploys are
# allowed to finish rather than being cancelled, so the live site is never
# left half-published.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - uses: actions/configure-pages@v5
      - run: bun install --frozen-lockfile || bun install
      # vite.config.ts already defaults base to /wcc-emergency-gis-showcase/,
      # which is the path Pages serves this project under, so assets resolve.
      - run: bun run build
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
